plugins {
    id "org.sonarqube" version "2.2.1"
}

sonarqube {
    properties {
        property "sonar.projectKey", "manishkumar1509_bahmni-package"
        property "sonar.organization", "bahmni-package"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}

repositories {
    mavenCentral()
}
